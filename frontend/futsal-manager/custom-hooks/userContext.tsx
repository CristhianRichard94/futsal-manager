// Deprecated: the legacy localStorage-backed user context has been replaced
// by NextAuth session state (see app/providers.tsx and lib/authOptions.ts).
// This file is intentionally left empty (no exports) to avoid an unused
// dead-code module while keeping history reviewable; nothing imports it.
export {};
